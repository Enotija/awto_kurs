{
  "name": "awto-szkola",
  "private": true,
  "version": "0.1.0",
  "description": "3D-симулятор вождения для подготовки к польскому экзамену на права категории B",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview --open",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
